services:
  site:
    image: nginx:alpine
    container_name: thomasblalock-site
    ports:
      - "8080:80"
    volumes:
      - .:/srv/site:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
